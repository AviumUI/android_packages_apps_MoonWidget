<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_surface"
    android:contentDescription="@string/countdown_description"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="18dp">

    <TextView
        android:id="@+id/text_countdown_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:text="@string/countdown_default_title"
        android:textColor="@color/widget_primary"
        android:textSize="15sp" />

    <TextView
        android:id="@+id/text_countdown_days"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif"
        android:text="@string/countdown_today"
        android:textColor="@color/widget_on_surface"
        android:textSize="34sp"
        android:textStyle="bold" />
</LinearLayout>
